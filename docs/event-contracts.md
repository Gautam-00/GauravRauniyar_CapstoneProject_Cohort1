# Event Contracts

This document will contain the actual RabbitMQ event contracts, primarily detailing the structure of async messages (e.g., `ORDER_COMPLETED`).

## TODO
- [ ] Document `ORDER_COMPLETED` event payload and routing keys
