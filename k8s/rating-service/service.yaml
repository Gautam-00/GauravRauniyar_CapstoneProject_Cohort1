apiVersion: v1
kind: Service
metadata:
  name: rating-service
  namespace: cake-delight
  labels:
    app: rating-service
spec:
  type: ClusterIP
  selector:
    app: rating-service
  ports:
    - port: 3003
      targetPort: 3003
